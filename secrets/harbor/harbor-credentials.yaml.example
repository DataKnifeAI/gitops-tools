# Harbor Credentials Secret
# This is a reference template - copy and customize for your use
# DO NOT commit actual secrets to git
#
# Usage:
# 1. Copy this file: cp secrets/harbor/harbor-credentials.yaml.example secrets/harbor/harbor-credentials.yaml
# 2. Edit secrets/harbor/harbor-credentials.yaml with your actual passwords
# 3. Apply: kubectl apply -f secrets/harbor/harbor-credentials.yaml

apiVersion: v1
kind: Secret
metadata:
  name: harbor-credentials
  namespace: managed-tools
type: Opaque
stringData:
  # Harbor admin password - CHANGE THIS!
  harborAdminPassword: "CHANGE_ME"
  # Database password - CHANGE THIS!
  databasePassword: "CHANGE_ME"
  # Redis password (optional, leave empty if not using)
  redisPassword: ""
