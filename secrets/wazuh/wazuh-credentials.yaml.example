# Wazuh Credentials Secret
# This is a reference template - copy and customize for your use
# DO NOT commit actual secrets to git
#
# Usage:
# 1. Copy this file: cp secrets/wazuh/wazuh-credentials.yaml.example secrets/wazuh/wazuh-credentials.yaml
# 2. Edit secrets/wazuh/wazuh-credentials.yaml with your actual passwords
# 3. Apply: kubectl apply -f secrets/wazuh/wazuh-credentials.yaml
#
# Alternatively, use the generate-wazuh-certificates.sh script which creates this secret automatically

apiVersion: v1
kind: Secret
metadata:
  name: wazuh-credentials
  namespace: managed-tools
type: Opaque
stringData:
  # Indexer admin password - CHANGE THIS!
  indexer-password: "CHANGE_ME"
  # Server API password - CHANGE THIS!
  server-password: "CHANGE_ME"
  # Dashboard admin password - CHANGE THIS!
  dashboard-password: "CHANGE_ME"
