# Loki Ingress
#
# Exposes Loki API and UI via ingress for external access
# Access Loki at https://loki.dataknife.net
# - Push traffic (Alloy/Promtail) -> loki-distributor
# - Query traffic (Grafana, UI) -> loki-query-frontend
#
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: loki
  namespace: grafana
  labels:
    app: loki
    component: loki
    managed-by: gitops
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    # Using default ingress certificate - no explicit TLS secret needed
spec:
  ingressClassName: nginx
  rules:
    - host: loki.dataknife.net
      http:
        paths:
          - path: /loki/api/v1/push
            pathType: Prefix
            backend:
              service:
                name: loki-distributor
                port:
                  number: 3100
          - path: /
            pathType: Prefix
            backend:
              service:
                name: loki-query-frontend
                port:
                  number: 3100
