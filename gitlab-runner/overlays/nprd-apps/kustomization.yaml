apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: managed-cicd

resources:
  - gitlab-runner-helmchart.yaml
  # Harbor (harbor.dataknife.net) uses a public TLS chain. Node and job trust rely on
  # default CA bundles — no privileged DaemonSet or harbor-ca-cert secret in GitOps.

# Cluster-specific overrides for nprd-apps
# Add any nprd-apps specific configurations here
# Examples:
# - resource limits/requests
# - storage class
# - environment-specific values
# - GitLab URL (if different per cluster)

patches:
  # Add patches here for nprd-apps specific configurations
  # - path: resource-limits.yaml
  #   target:
  #     kind: HelmChart
  #     name: gitlab-runner
