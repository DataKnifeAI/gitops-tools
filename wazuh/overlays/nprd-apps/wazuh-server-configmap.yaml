# Wazuh Server ConfigMap
#
# This is a placeholder. Replace with actual Wazuh Server configuration
# from the official wazuh-kubernetes repository.
#
# Key configuration:
# - Connection to Wazuh Indexer (service: wazuh-indexer:9200)
# - Agent enrollment settings
# - Rule and decoder paths
# - Active response configuration
#
apiVersion: v1
kind: ConfigMap
metadata:
  name: wazuh-server-config
  namespace: managed-tools
  labels:
    app: wazuh
    component: server
    managed-by: gitops
data:
  ossec.conf: |
    <!-- Wazuh Server configuration -->
    <!-- This should be populated from official Wazuh Kubernetes manifests -->
    <!-- Must include connection to wazuh-indexer service -->
