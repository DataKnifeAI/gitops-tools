# Wazuh Indexer ConfigMap
# 
# This is a placeholder. Replace with actual Wazuh Indexer configuration
# from the official wazuh-kubernetes repository.
#
# To get the actual manifest:
# 1. Clone https://github.com/wazuh/wazuh-kubernetes.git
# 2. Review the indexer configuration files
# 3. Adapt them to use:
#    - namespace: managed-tools
#    - labels: app: wazuh, component: indexer, managed-by: gitops
#    - storage class: truenas-nfs (configured in overlay)
#
# Example structure (adapt from official manifests):
apiVersion: v1
kind: ConfigMap
metadata:
  name: wazuh-indexer-config
  namespace: managed-tools
  labels:
    app: wazuh
    component: indexer
    managed-by: gitops
data:
  # Add Wazuh Indexer configuration here
  # Refer to official Wazuh documentation for required settings
  opensearch.yml: |
    # Wazuh Indexer configuration
    # This should be populated from official Wazuh Kubernetes manifests
