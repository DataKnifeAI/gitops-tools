apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: managed-tools

resources:
  # Base resources embedded in overlay for Fleet path resolution
  # Note: namespace.yaml removed - managed-tools namespace already exists
  - harbor-helmchart.yaml
  - postgresql-cluster.yaml
  - postgresql-database.yaml

commonLabels:
  app: harbor
  managed-by: gitops
