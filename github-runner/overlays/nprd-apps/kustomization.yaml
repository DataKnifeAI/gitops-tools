apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: managed-cicd

resources:
  # Runner Scale Set Definition
  # NOTE: The controller (gha-runner-scale-set-controller) is managed by Terraform
  # This repository only manages runner definitions (AutoscalingRunnerSet)
  - autoscalingrunnerset.yaml

# Cluster-specific overrides for nprd-apps
# Add any nprd-apps specific configurations here
# Examples:
# - resource limits/requests
# - storage class
# - environment-specific values

patches:
  # Add patches here for nprd-apps specific configurations
  # - path: resource-limits.yaml
  #   target:
  #     kind: AutoscalingRunnerSet
  #     name: github-runner-scale-set
