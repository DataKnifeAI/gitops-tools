apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: managed-cicd

# Base configuration for GitHub Actions Runner Controller
# This is a base template - use overlays for cluster-specific configurations
#
# Official GitHub-supported ARC uses AutoscalingRunnerSet
# See overlays/nprd-apps for cluster-specific configurations

resources:
  # Base Fleet configuration
  - fleet.yaml

commonLabels:
  app: github-runner
  managed-by: gitops
